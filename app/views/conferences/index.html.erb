<p id="notice"><%= notice %></p>

<h1>Listing Conferences</h1>

<table>
  <thead>
    <tr>
      <th>Start date</th>
      <th>End date</th>
      <th>Max team size</th>
      <th>Min team size</th>
      <th>Max teams</th>
      <th>Tamu cost</th>
      <th>Other cost</th>
      <th>Challenge desc</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @conferences.each do |conference| %>
      <tr>
        <td><%= conference.start_date %></td>
        <td><%= conference.end_date %></td>
        <td><%= conference.max_team_size %></td>
        <td><%= conference.min_team_size %></td>
        <td><%= conference.max_teams %></td>
        <td><%= conference.tamu_cost %></td>
        <td><%= conference.other_cost %></td>
        <td><%= conference.challenge_desc %></td>
        <td><%= link_to 'Show', conference %></td>
        <td><%= link_to 'Edit', edit_conference_path(conference) %></td>
        <td><%= link_to 'Destroy', conference, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Conference', new_conference_path %>
