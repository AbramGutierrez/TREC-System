.row
	%div{:class => "small-12 text-center columns"}
		
		%h2 Log in

		= form_tag sessions_path, :method => :post do

			= label :session, :email, 'Email', :class => "text-left"
			= text_field :session, :email
			
			= label :session, :password, 'Password', :class => "text-left"
			= text_field :session, :password
			
			= submit_tag 'Log in', :class => "button round"