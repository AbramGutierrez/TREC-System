<!-- Commented this notice to prevent from displaying twice, later delete it

<p id="notice"><%= notice %></p>

-->

<p>
  <strong>Team name:</strong>
  <%= @team.team_name %>
</p>

<p>
  <strong>Paid status:</strong>
  <%= @team.paid_status %>
</p>

<p>
  <strong>School:</strong>
  <%= @team.school %>
</p>

<% if is_team_captain? || is_admin? %>
<%= link_to 'Edit Team Info', edit_team_path(@team) %> |
<% @team_id = @team.id %>
<%= link_to "Add/Remove Team Members", controller: "team_members", action: "show", id: @team_id %> |
<% end %>

<%= link_to 'Back', :back %>
