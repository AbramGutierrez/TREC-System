<%= form_for(@pageinfo) do |f| %>
  <% if @pageinfo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pageinfo.errors.count, "error") %> prohibited this pageinfo from being saved:</h2>

      <ul>
      <% @pageinfo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :page %><br>
    <%= f.text_field :page %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
